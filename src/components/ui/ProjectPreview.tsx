// Intentionally empty — project images are now wired directly in ProjectRow.tsx
// using existing git-history screenshots (see IMAGE_BY_PROJECT map).
// Kept as a placeholder file so we don't churn imports if we ever need it back.
export {};
